.row
  .col-md-12
    = link_to 'Новый пользователь', new_user_path, class: 'btn btn-primary btn-xs'

- if @users.any?
  %hr
  .row
    .col-md-5
      %ul
        - @users.each do |user|
          %li= link_to user.full_name, user_path(user)
